# VeriMind-Med Backend Dependencies
# ── Web 框架 ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
pydantic-settings>=2.0

# ── LLM 客户端 (OpenAI 兼容协议, 覆盖智谱/DashScope/DeepSeek) ──
openai>=1.30.0

# ── 向量数据库 ──
chromadb>=0.5.0

# ── PDF 解析 ──
pymupdf>=1.24.0
pymupdf4llm>=0.0.10
pdfplumber>=0.11.0

# ── Agent 编排 (阶段三引入) ──
langgraph>=0.2.0
langchain-core>=0.3.0

# ── 工具库 ──
python-dotenv>=1.0.0
loguru>=0.7.0
httpx>=0.27.0
